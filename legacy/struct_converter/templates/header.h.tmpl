#ifndef ${header_guard}
#define ${header_guard}

#include <cjson/cJSON.h>
#include <stddef.h>
#include <stdbool.h>

// 转换状态码
typedef enum {
    CONVERT_SUCCESS = 0,
    CONVERT_MALLOC_ERROR,
    CONVERT_PARSE_ERROR,
    CONVERT_INVALID_PARAM
} convert_status_t;

${declarations}

#endif // ${header_guard} 